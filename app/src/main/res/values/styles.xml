<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base"/>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="DatePickerStyle">
        <item name="lineSpace">16dp</item>
        <item name="textSize">24sp</item>
        <item name="selectedColor">#333333</item>
        <item name="unselectedColor">#666666</item>
        <item name="dividerTop">@drawable/date_picker_divider</item>
        <item name="dividerBottom">@drawable/date_picker_divider</item>
    </style>
</resources>
